name: Playwright Tests
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    name: ${{ matrix.browser }} - Shard ${{ matrix.shard }}/4
    timeout-minutes: 60
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # ==============================
          # Chromium
          # ==============================
          - browser: Chromium
            install-browser: chromium
            shard: 1
          - browser: Chromium
            install-browser: chromium
            shard: 2
          - browser: Chromium
            install-browser: chromium
            shard: 3
          - browser: Chromium
            install-browser: chromium
            shard: 4
          # ==============================
          # Firefox
          # ==============================
          - browser: Firefox
            install-browser: firefox
            shard: 1
          - browser: Firefox
            install-browser: firefox
            shard: 2
          - browser: Firefox
            install-browser: firefox
            shard: 3
          - browser: Firefox
            install-browser: firefox
            shard: 4
          # ==============================
          # WebKit
          # ==============================
          - browser: WebKit
            install-browser: webkit
            shard: 1
          - browser: WebKit
            install-browser: webkit
            shard: 2
          - browser: WebKit
            install-browser: webkit
            shard: 3
          - browser: WebKit
            install-browser: webkit
            shard: 4
    env:
      CI: true
      PW_WORKERS: 4
      TEST_ENV: QA
    steps:
      # ==============================
      # Checkout
      # ==============================
      - name: Checkout repository
        uses: actions/checkout@v4
      # ==============================
      # Node.js
      # ==============================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
      # ==============================
      # Dependencies
      # ==============================
      - name: Install dependencies
        run: npm ci
      # ==============================
      # Playwright Browser Cache
      # ==============================
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ matrix.install-browser }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-${{ matrix.install-browser }}-
      # ==============================
      # Install only required browser
      # ==============================
      - name: Install Playwright Browser
        run: npx playwright install ${{ matrix.install-browser }}
      # ==============================
      # Run Tests
      # ==============================
      - name: Run Playwright tests
        run: |
          npx playwright test \
            --project=${{ matrix.browser }} \
            --shard=${{ matrix.shard }}/4 \
            --workers=${{ env.PW_WORKERS }}
      # ==============================
      # Upload Playwright Blob Report
      # ==============================
      - name: Upload Playwright Blob Report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-blob-${{ matrix.browser }}-${{ matrix.shard }}
          path: blob-report/
          retention-days: 30
      # ==============================
      # Upload Allure Results
      # ==============================
      - name: Upload Allure Results
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ matrix.browser }}-${{ matrix.shard }}
          path: allure-results/
          retention-days: 30
  # ============================================================
  # MERGE REPORTS
  # ============================================================
  merge-reports:
    name: Merge Test Reports
    if: ${{ !cancelled() }}
    needs: test
    runs-on: ubuntu-latest
    steps:
      # ==============================
      # Checkout
      # ==============================
      - name: Checkout repository
        uses: actions/checkout@v4
      # ==============================
      # Node.js
      # ==============================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
      # ==============================
      # Dependencies
      # ==============================
      - name: Install dependencies
        run: npm ci
      # ==============================
      # Download Playwright Blob Reports
      # ==============================
      - name: Download Playwright Blob Reports
        uses: actions/download-artifact@v4
        with:
          pattern: playwright-blob-*
          path: blob-reports
          merge-multiple: true
      # ==============================
      # Merge Playwright HTML Report
      # ==============================
      - name: Merge Playwright HTML Report
        run: |
          npx playwright merge-reports \
            --reporter html \
            blob-reports
      # ==============================
      # Upload Merged Playwright Report
      # ==============================
      - name: Upload Merged Playwright Report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      # ==============================
      # Download Allure Results
      # ==============================
      - name: Download Allure Results
        uses: actions/download-artifact@v4
        with:
          pattern: allure-results-*
          path: allure-results
          merge-multiple: true
      # ==============================
      # Generate Allure Report
      # ==============================
      - name: Generate Allure Report
        run: |
          npx allure generate \
            allure-results \
            -o allure-report
      # ==============================
      # Upload Merged Allure Report
      # ==============================
      - name: Upload Merged Allure Report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/
          retention-days: 30